package math;

//import geometry.Circle;
import geometry.*;
import geometry.polygon.*;

public class GeometyPolygonMain {
	public static void main(String[] args) {
		Circle c = new Circle(5);
		Rectangle r = new Rectangle(2,3);
		Square s = new Square(5);
	}
}
